<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
  </head>
</html>

<div class="article-page">
  <%= render 'layouts/navbar' %>
  <div class="banner">
    <div class="container">
      <h1>
        <%= @post.title %>
      </h1>

      <div class="article-meta">
        <a href="/profile/eric-simons"><img src="http://i.imgur.com/Qr71crq.jpg" /></a>
        <div class="info">
          <a href="/profile/eric-simons" class="author">
            <%= @user.name %>
          </a>
          <span class="date">
            <%= @post.created_at %>
          </span>
        </div>
        <button class="btn btn-sm btn-outline-secondary">
          <i class="ion-plus-round"></i>
          ＋ Follow <%= @user.name %>
        </button>
        <button class="btn btn-sm btn-outline-primary">
          <i class="ion-heart"></i>
          ♥ Favorite Post
          <span class="counter">
            (<%= @post.likes %>)
          </span>
        </button>
        <%= link_to edit_post_path(@post), class: "btn btn-sm btn-outline-secondary" do %>
          <i class="ion-edit"></i> Edit Article
        <% end %>


        <button class="btn btn-sm btn-outline-danger">
          <i class="ion-trash-a"></i> Delete Article
        </button>
      </div>
    </div>
  </div>

  <div class="container page">
    <div class="row article-content">
      <div class="col-md-12">
        <p>
          <%= @post.content %>
        </p>
        <div class="tag-list">
          <% @categories.each do |category| %>
            <span class="tag-default tag-pill" data-category="<%= category.name %>"><i class="ion-close-round"></i> <%= category.name %></span>
          <% end %>
        </div>
      </div>
    </div>

    <hr />

    <div class="article-actions">
      <div class="article-meta">
        <a href="profile.html"><img src="http://i.imgur.com/Qr71crq.jpg" /></a>
        <div class="info">
          <a href="" class="author">Eric Simons</a>
          <span class="date">January 20th</span>
        </div>

        <button class="btn btn-sm btn-outline-secondary">
          <i class="ion-plus-round"></i>
          ＋ Follow <%= @user.name %>
        </button>
        <button class="btn btn-sm btn-outline-primary">
          <i class="ion-heart"></i>
          ♥ Favorite Post
          <span class="counter">
            (<%= @post.likes %>)
          </span>
        </button>
        <button class="btn btn-sm btn-outline-secondary">
          <i class="ion-edit"></i> Edit Article
        </button>
        <button class="btn btn-sm btn-outline-danger">
          <i class="ion-trash-a"></i> Delete Article
        </button>
      </div>
    </div>

    <div class="row">
      <div class="col-xs-12 col-md-8 offset-md-2">
        <form class="card comment-form">
          <div class="card-block">
            <textarea class="form-control" placeholder="Write a comment..." rows="3"></textarea>
          </div>
          <div class="card-footer">
            <img src="http://i.imgur.com/Qr71crq.jpg" class="comment-author-img" />
            <button class="btn btn-sm btn-primary">Post Comment</button>
          </div>
        </form>

        <% @comments.each do |comment| %>
          <div class="card mb-3">
            <div class="card-block">
              <p class="card-text"><%= comment.content %></p>
            </div>
            <div class="card-footer">
              <!-- コメントしたユーザーのプロフィールへのリンク -->
              <a href="/profile/<%= comment.user.username %>" class="comment-author">
                <!-- ユーザー画像、存在しない場合はデフォルト画像を使用 -->
                <% img_url = comment.user.image.presence || "http://i.imgur.com/Qr71crq.jpg" %>
                <img src="<%= img_url %>" class="comment-author-img" />
              </a>
              &nbsp;
              <a href="/profile/<%= comment.user.username %>" class="comment-author"><%= comment.user.name %></a>
              <!-- コメントの日付の表示、日付は適切にフォーマットする -->
              <span class="date-posted"><%= comment.created_at.strftime("%b %d, %Y") %></span>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
